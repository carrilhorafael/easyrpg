import { HeaderContainer, HeaderTitle } from "./Header.styles";

export default function Header() {

  return (
    <HeaderContainer>
      <HeaderTitle>EasyRPG</HeaderTitle>
    </HeaderContainer>
  );
}
